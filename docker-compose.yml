services:
  app:
    build:
      context: .
      args:
        VITE_VAPID_PUBLIC_KEY: ${VITE_VAPID_PUBLIC_KEY}
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
    networks:
      - web

networks:
  web:
    external: true
